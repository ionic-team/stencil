{
  "name": "ionic-next",
  "version": "0.0.1",
  "description": "yolo",
  "author": "yes",
  "license": "MIT Romney",
  "scripts": {
    "build": "npm run build.clean && npm run transpile && npm run copy.package.json",
    "build.clean": "rimraf dist",
    "build.quick": " npm run transpile",
    "copy.package.json": "cp scripts/npm.package.json dist/package.json",
    "transpile": "node scripts/transpile.js",
    "lint": "tslint --config src/tslint.json 'src/**/*.ts'",
    "validate": "npm run lint"
  },
  "devDependencies": {
    "rimraf": "2.5.4",
    "tslint": "4.3.1",
    "typescript": "2.2.0"
  }
}
