{
  "scripts": {
    "build": "node ../../bin/stencil build",
    "serve.express": "node ./server.express.js",
    "serve.node": "node ./server.node.js",
    "test": "node ../../node_modules/.bin/jest --no-cache",
    "test.prod": "npm install && npm run build && npm run test"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "express": "^4.16.3",
    "supertest": "^3.0.0"
  }
}
