{
  "name": "ionic-core",
  "version": "0.0.1",
  "description": "yolo",
  "author": "yes",
  "license": "MIT Romney",
  "scripts": {
    "build": "npm run build.clean && npm run build.index && npm run copy.package.json && npm run build.transpiler",
    "build.clean": "rimraf dist",
    "build.es2015": "tsc --project src",
    "build.commonjs": "tsc --project src --module commonjs --outDir dist/commonjs",
    "build.index": "npm run build.es2015 && npm run build.commonjs && npm run bundle.index",
    "build.transpiler": "tsc --project src/transpiler",
    "bundle.index": "node scripts/bundle.index.js",
    "build.quick": "npm run build.index",
    "copy.package.json": "cp scripts/npm.package.json dist/package.json",
    "test": "npm run build.index && (jest || true)",
    "lint": "tslint --config src/tslint.json 'src/**/*.ts'",
    "validate": "npm run test & npm run lint"
  },
  "devDependencies": {
    "@types/jest": "^18.1.1",
    "@types/node": "^7.0.5",
    "jest": "18.1.0",
    "jsdom": "^9.11.0",
    "rimraf": "2.5.4",
    "rollup": "0.41.4",
    "tslint": "4.3.1",
    "typescript": "2.2.0",
    "vue": "2.1.10",
    "vue-template-compiler": "2.1.10",
    "webpack": "^2.2.1"
  },
  "jest": {
    "testEnvironment": "node",
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "transform": {
      "^.+\\.(ts)$": "<rootDir>/scripts/preprocessor.js"
    },
    "testRegex": "/src/.*\\.spec\\.(ts|js)$"
  }
}
