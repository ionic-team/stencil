{
  "name": "ionic-next",
  "version": "0.0.1",
  "description": "yolo",
  "author": "yes",
  "license": "MIT Romney",
  "scripts": {
    "build": "npm run build.clean && npm run build.ts && npm run copy.package.json",
    "build.clean": "rimraf dist",
    "build.ts": "tsc --project src",
    "copy.package.json": "cp scripts/build/package.json dist",
    "generate": "node scripts/build/generate.js"
  },
  "devDependencies": {
    "rimraf": "2.5.4",
    "tslint": "4.3.1",
    "typescript": "2.2.0"
  }
}
