{
  "name": "ionic-vue",
  "private": true,
  "version": "0.0.1",
  "license": "MIT",
  "main": "es2015/index.js",
  "scripts": {
    "clean": "rimraf ../../../dist/transpiled-vue/",
    "build": "npm run transpile && node scripts/build.vue.js",
    "start": "npm install && npm run build",
    "transpile": "npm run clean && tsc -p tsconfig.json",
    "transpile.watch": "npm run clean && tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "rimraf": "2.6.1",
    "typescript": "2.2.1"
  }
}
